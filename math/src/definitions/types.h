#pragma once

#include <cmath>
#include <cstdint>

namespace math {

	template<int S, typename T> 
	struct Vec;
	
	template<int C, int R, typename T> 
	struct Mat;

	template<typename T> 
	struct Quat;

}